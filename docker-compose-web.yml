version: '3.8'

services:
  web:
    image: tomcat:9.0-jdk8
    container_name: oa-web
    restart: always
    ports:
      - "8080:8080"
    volumes:
      - ./target/simulation-oa.war:/usr/local/tomcat/webapps/ROOT.war
    environment:
      - DB_URL=jdbc:mysql://10.11.2.110:3306/scrm_zhongxin?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
      - DB_USERNAME=root
      - DB_PASSWORD=123456